# Backend
BACKEND_PORT=8000

# PostgreSQL Database (desenvolvimento local)
POSTGRES_USER=recicla_user
POSTGRES_PASSWORD=recicla_pass
POSTGRES_DB=recicla_ai
POSTGRES_PORT=5432

# Database URL (gerada automaticamente pelo Docker Compose)
# Não é necessário definir aqui, mas você pode sobrescrever se quiser
# DATABASE_URL=postgresql://recicla_user:recicla_pass@localhost:5432/recicla_ai

# TOKEN-KEY
API_KEY=SUPER_SECRET_API_KEY_12345